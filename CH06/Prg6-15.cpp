/*
#include <iostream>
using namespace std;

void swap(int& first, int& second);

int main(void)
{
    int first = 10;
    int second = 20;
    swap(first, second);
    
    cout << "fist in main : " << first << endl;
    cout << "second in main : " << second << endl;
    
    return 0;
}
void swap(int& fst, int& scd)
{
    int tmp = fst;
    fst = scd;
    scd = tmp;
    return;
}
*/
