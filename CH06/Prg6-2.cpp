/*
#include <iostream>
#include <cmath>
using namespace std;

int main(void)
{
    int a = 0;
    int b = 0;
    int c = 0;
    
    double term;
    
    // get a, b, c
    cout << " a : ";
    cin >> a;
    cout << " b : ";
    cin >> b;
    cout << " c : ";
    cin >> c;
    
    // get term
    term = pow(b,2) - 4*a*c;
    
    // discriminant
    if(term==0)
    {
        cout << "one" << endl;
    }
    else if (term > 0)
    {
        cout << "two" << endl;
    }
    else
    {
        cout << "None" << endl;
    }
    
    return 0;
}
*/
