/*
#include <iostream>
#include <cctype>
using namespace std;

int main(void)
{
    char ch;
    int count = 0;
    
    while(cin >> noskipws >> ch)
    {
        if(isalpha(ch))
        {
            count++;
        }
        ch = toupper(ch);
        cout << ch;
    }
    cout << "The number of Alphabet character : " << count << endl;
    return 0;
}
*/
